# CONFIGURACIÓN DEL SERVIDOR
NODE_ENV=development
PORT=5000

# CONFIGURACIÓN DE BASE DE DATOS PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_NAME=practicas_espoch
DB_USER=postgres
DB_PASSWORD=admin

# JWT SECRET (Cambiar en producción - Generar con: openssl rand -base64 32)
JWT_SECRET=tu_secret_key_super_segura_aqui_cambiar_en_produccion
JWT_EXPIRE=7d

# CONFIGURACIÓN DE EMAIL (Nodemailer con Gmail)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=tu_email@gmail.com
EMAIL_PASSWORD=tu_app_password_aqui
EMAIL_FROM=Sistema Prácticas ESPOCH <noreply@espoch.edu.ec>

# URL DEL FRONTEND (para CORS)
FRONTEND_URL=http://localhost:5173

# CONFIGURACIÓN DE ARCHIVOS
MAX_FILE_SIZE=5242880
ALLOWED_FILE_TYPES=.pdf,.doc,.docx